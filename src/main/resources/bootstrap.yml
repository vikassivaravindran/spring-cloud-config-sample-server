spring : 
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:   https://github.com/vikassivaravindran/spring-cloud-config-repo.git 
server:
  port : 8095 